@model AppData.Data.Sklep.Kategoria

@{
    ViewData["Title"] = "Edytuj kategorię";
}

<div class="container py-5 min-vh-100">

    <div class="text-center mb-5">
        <h1 class="display-4 fw-bold">
            <i class="bi bi-pencil-square me-2"></i> Edytuj kategorię
        </h1>
        <p class="text-muted">Zmieniaj szczegóły wybranej kategorii</p>
    </div>

    <div class="row justify-content-center">
        <div class="col-lg-6 col-md-8">

            <form asp-action="Edit" novalidate>
                <input type="hidden" asp-for="KategoriaID" />

                <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                <div class="mb-4">
                    <label asp-for="Nazwa" class="form-label fw-semibold"></label>
                    <input asp-for="Nazwa" class="form-control form-control-lg" />
                    <span asp-validation-for="Nazwa" class="text-danger"></span>
                </div>

                <div class="mb-4">
                    <label asp-for="Opis" class="form-label fw-semibold"></label>
                    <input asp-for="Opis" class="form-control form-control-lg" />
                    <span asp-validation-for="Opis" class="text-danger"></span>
                </div>

                <div class="d-flex justify-content-between align-items-center">
                    <button type="submit" class="btn btn-primary btn-lg d-flex align-items-center gap-2 shadow-sm">
                        <i class="bi bi-check2-circle"></i> Zapisz zmiany
                    </button>
                    <a asp-action="Index" class="btn btn-outline-secondary btn-lg d-flex align-items-center gap-2 shadow-sm">
                        <i class="bi bi-x-circle"></i> Anuluj
                    </a>
                </div>
            </form>

        </div>
    </div>

</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
