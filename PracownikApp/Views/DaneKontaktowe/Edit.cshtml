@model AppData.Data.Sklep.DaneKontaktowe

@{
    ViewData["Title"] = "Edytuj dane kontaktowe";
}

<div class="container py-5 min-vh-100">

    <div class="text-center mb-5">
        <h1 class="display-4 fw-bold">
            <i class="bi bi-person-lines-fill me-2"></i> Edytuj dane kontaktowe
        </h1>
        <p class="text-muted">Zaktualizuj informacje kontaktowe użytkownika</p>
    </div>

    <div class="row justify-content-center">
        <div class="col-lg-6 col-md-8">

            <form asp-action="Edit" novalidate>
                <input type="hidden" asp-for="UzytkownikID" />

                <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                <div class="mb-4">
                    <label asp-for="Adres" class="form-label fw-semibold"></label>
                    <input asp-for="Adres" class="form-control form-control-lg" />
                    <span asp-validation-for="Adres" class="text-danger"></span>
                </div>

                <div class="mb-4">
                    <label asp-for="KodPocztowy" class="form-label fw-semibold"></label>
                    <input asp-for="KodPocztowy" class="form-control form-control-lg" />
                    <span asp-validation-for="KodPocztowy" class="text-danger"></span>
                </div>

                <div class="mb-4">
                    <label asp-for="Miasto" class="form-label fw-semibold"></label>
                    <input asp-for="Miasto" class="form-control form-control-lg" />
                    <span asp-validation-for="Miasto" class="text-danger"></span>
                </div>

                <div class="mb-4">
                    <label asp-for="Telefon" class="form-label fw-semibold"></label>
                    <input asp-for="Telefon" class="form-control form-control-lg" />
                    <span asp-validation-for="Telefon" class="text-danger"></span>
                </div>

                <div class="d-flex justify-content-between align-items-center">
                    <button type="submit" class="btn btn-primary btn-lg d-flex align-items-center gap-2 shadow-sm">
                        <i class="bi bi-check2-circle"></i> Zapisz zmiany
                    </button>
                    <a asp-action="Index" class="btn btn-outline-secondary btn-lg d-flex align-items-center gap-2 shadow-sm">
                        <i class="bi bi-x-circle"></i> Anuluj
                    </a>
                </div>
            </form>

        </div>
    </div>

</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
