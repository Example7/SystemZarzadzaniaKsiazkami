@model AppData.Data.Sklep.Wydawnictwo

@{
    ViewData["Title"] = "Szczegóły wydawnictwa";
}

<div class="container py-5 min-vh-100">

    <section class="text-center mb-5">
        <div class="bg-info bg-gradient text-white rounded-5 shadow-lg py-4 px-3">
            <h1 class="display-4 fw-bold">
                <i class="bi bi-building me-3"></i> Szczegóły wydawnictwa
            </h1>
            <p class="lead mb-0">Szczegółowy podgląd informacji o wydawnictwie</p>
        </div>
    </section>

    <article class="card shadow rounded-4 border-0 mx-auto" style="max-width: 700px;">
        <div class="rounded-top" style="height: 6px; background: linear-gradient(to right, #0dcaf0, #6610f2);"></div>
        <div class="card-body px-5 py-4">
            <h2 class="card-title fw-semibold mb-4 text-primary">@Model.Nazwa</h2>

            <dl class="row mb-5">
                <dt class="col-md-4 text-muted fw-semibold">Nazwa</dt>
                <dd class="col-md-8 fs-5">@Model.Nazwa</dd>

                <dt class="col-md-4 text-muted fw-semibold align-self-start">Opis</dt>
                <dd class="col-md-8 fs-6" style="white-space: pre-wrap; line-height: 1.4;">
                    @if (!string.IsNullOrWhiteSpace(Model.Opis))
                    {
                        @Html.Raw(Model.Opis)
                    }
                    else
                    {
                        <span class="text-secondary fst-italic">Brak opisu do wyświetlenia</span>
                    }
                </dd>
            </dl>

            <div class="d-flex justify-content-start gap-3">
                <a asp-action="Edit" asp-route-id="@Model.WydawnictwoID" class="btn btn-outline-info shadow-sm d-flex align-items-center gap-2">
                    <i class="bi bi-pencil-square"></i> Edytuj wydawnictwo
                </a>
                <a asp-action="Index" class="btn btn-outline-secondary shadow-sm d-flex align-items-center gap-2">
                    <i class="bi bi-arrow-left-circle"></i> Wróć do listy
                </a>
            </div>
        </div>
    </article>

</div>
